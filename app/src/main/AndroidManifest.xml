<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icone"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icone"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ChargementActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".Production2" />
        <activity android:name=".Prix_unitaire" />
        <activity android:name=".Vente" />
        <activity android:name=".Production" />
        <activity android:name=".BonAchat" />
        <activity android:name=".Bon_de_commande" />
        <activity android:name=".MainActivity" />

    </application>

</manifest>